# ============================================================
# telegram_claude_bot 환경 설정 파일
# ============================================================
# 사용법:
#   1. 이 파일을 복사하여 .env 로 저장하세요.
#      Windows: copy .env.example .env
#      Linux:   cp .env.example .env
#   2. 아래 값들을 실제 값으로 채워 넣으세요.
#   3. .env 파일은 절대 git에 커밋하지 마세요! (.gitignore에 포함됨)
# ============================================================


# ============================================================
# [1] Telegram Bot 설정
# ============================================================

# ── TELEGRAM_BOT_TOKEN ──────────────────────────────────────
# 텔레그램 봇의 인증 토큰입니다.
#
# 발급 방법:
#   1. 텔레그램 앱에서 @BotFather 를 검색하여 대화를 시작합니다.
#   2. /newbot 명령을 입력합니다.
#   3. 봇 이름(표시 이름)을 입력합니다. 예: My Claude Bot
#   4. 봇 사용자명(username)을 입력합니다. 예: my_claude_bot
#      (반드시 'bot'으로 끝나야 합니다)
#   5. BotFather가 토큰을 발급해 줍니다.
#      형식: 1234567890:ABCdefGHIjklMNOpqrSTUvwxYZ
#
# 주의: 토큰은 비밀번호와 같습니다. 절대 외부에 노출하지 마세요.
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# ── TELEGRAM_CHAT_ID ────────────────────────────────────────
# 봇과 대화할 수 있는 허용된 사용자(또는 그룹)의 Chat ID 목록입니다.
# 설정하지 않으면 누구나 봇을 사용할 수 있어 위험합니다.
#
# 내 Chat ID 확인 방법 (방법 1 - 추천):
#   1. 텔레그램에서 @userinfobot 을 검색하여 대화를 시작합니다.
#   2. /start 또는 아무 메시지나 보내면 내 Chat ID를 알려줍니다.
#      예: Your user ID: 123456789
#
# 내 Chat ID 확인 방법 (방법 2):
#   1. 봇을 배포한 뒤 봇에게 아무 메시지나 보냅니다.
#   2. 브라우저에서 아래 URL을 열어 Chat ID를 확인합니다:
#      https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates
#      응답의 "from" > "id" 값이 Chat ID입니다.
#
# 그룹 채팅 ID 확인 방법:
#   1. 봇을 그룹에 추가합니다.
#   2. 위 방법 2의 URL에서 "chat" > "id" 값을 확인합니다.
#      그룹 ID는 보통 음수입니다. 예: -1001234567890
#
# 형식: Python 리스트 형태로 대괄호 안에 쉼표로 구분하여 입력
#   단일 사용자: [123456789]
#   다수 허용:   [123456789, 987654321]
#   그룹 포함:   [123456789, -1001234567890]
TELEGRAM_CHAT_ID=[123456789]

# 노션 토큰
NOTION_TOKEN=

# ============================================================
# [2] Claude Code 설정
# ============================================================

# ── CLAUDE_CODE_PATH ────────────────────────────────────────
# Claude Code CLI 실행 파일 경로입니다.
# PATH에 등록된 경우 'claude' 만 입력하면 됩니다.
# 직접 경로 지정 예:
#   Windows: C:/Users/user/AppData/Roaming/npm/claude.cmd
#   Linux:   /usr/local/bin/claude
#
# Claude Code CLI 설치 방법:
#   1. Node.js가 설치되어 있어야 합니다. (https://nodejs.org/)
#   2. 터미널에서 실행: npm install -g @anthropic-ai/claude-code
#   3. 설치 후 인증: claude
#      (브라우저가 열리면 Anthropic 계정으로 로그인)
CLAUDE_CODE_PATH=claude

# ── DEFAULT_MODEL ───────────────────────────────────────────
# 기본으로 사용할 Claude 모델명입니다.
# 사용 가능한 모델 (2025년 기준):
#   claude-opus-4-6       ← 가장 강력, 느리고 비쌈
#   claude-sonnet-4-6     ← 균형잡힌 성능 (권장)
#   claude-haiku-4-5      ← 빠르고 저렴, 간단한 작업용
#
# 인스턴스별로 /setmodel 명령으로 변경 가능합니다.
DEFAULT_MODEL=claude-sonnet-4-6

# ── DEFAULT_SESSION_NAME ─────────────────────────────────────
# 기본 Claude 세션의 표시 이름입니다. (.env에서만 변경 가능)
# /session 목록에서 표시되며, 이름 라우팅 시 이 이름으로 참조됩니다.
# 기본값: suho
DEFAULT_SESSION_NAME=suho


# ── DATABASE_PATH (선택) ─────────────────────────────────────
# 기본값: ./telegram_claude_bot.db (봇 실행 위치 기준)
# 변경이 필요한 경우에만 아래 주석을 해제하고 경로를 입력하세요.
#   Windows: DATABASE_PATH=C:/Users/user/bot_data/telegram_claude_bot.db
#   Docker:  DATABASE_PATH=/app/data/.db/telegram_claude_bot.db
# DATABASE_PATH=./telegram_claude_bot.db




# ============================================================
# [5] 사전 프롬프트 (System Prompt) 설정
# ============================================================
#
# 모든 메시지 앞에 자동으로 삽입되는 시스템 지시문입니다.
# Claude의 역할이나 행동 방식을 사전에 정의할 때 사용합니다.
# 비워두면 사전 프롬프트 없이 동작합니다.
#
# ── 단일 프롬프트 ────────────────────────────────────────────
# SYSTEM_PROMPT 하나만 사용하는 경우:
#
#   SYSTEM_PROMPT=You are a helpful assistant. Always respond in Korean.
#
# ── 다중 프롬프트 ────────────────────────────────────────────
# 역할을 여러 항목으로 분리하여 관리하고 싶을 때
# SYSTEM_PROMPT_1, SYSTEM_PROMPT_2, ... 형식으로 설정하세요.
# 번호 순서대로 빈 줄로 구분하여 합쳐집니다.
#
# 예시:
#   SYSTEM_PROMPT_1=You are a Project Manager (PM). Break down requests into structured tasks and delegate to agents.
#   SYSTEM_PROMPT_2=Always respond in the same language as the user.
#   SYSTEM_PROMPT_3=Do NOT perform implementation directly. Only plan, delegate, and review.
#
# 주의:
#   - 각 항목은 한 줄로 작성하세요.
#   - SYSTEM_PROMPT와 SYSTEM_PROMPT_1~N을 동시에 사용하면 모두 합쳐집니다.
#   - 번호는 반드시 1부터 연속되어야 합니다. (빈 번호가 있으면 이후는 무시)

SYSTEM_PROMPT_1=You are a highly capable Task Manager. Analyze each request, select the most appropriate agent(s), and organize them into an effective team. Delegate tasks clearly, coordinate progress, and synthesize results. Never perform implementation yourself—plan, delegate, and review.
